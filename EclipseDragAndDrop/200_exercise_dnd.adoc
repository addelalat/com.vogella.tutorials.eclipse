== Exercise: Drag and Drop in SWT

The methods `moveAbove` and `moveBelow` provide the option to re-arrange widgets within a layout.
The following example demonstrates the usage of drag and drop to re-arrange widgets in a layout.
It assumes that you have created several png file in an `images` folder of the project.


Create the following class. 
	
[source,java]
----
include::res/dndswt/PhotoShuffler.java[]
----

image::photodnd[photodnd] 
	
